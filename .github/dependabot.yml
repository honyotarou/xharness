# Supply-chain visibility (Layer 3 in common OSS hardening guides). Does not replace human review.
version: 2
updates:
  - package-ecosystem: "nuget"
    directory: "/"
    schedule:
      interval: "weekly"
    open-pull-requests-limit: 5
